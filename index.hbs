{{!< default}}

<div id="entries" class="mCont">
  {{#foreach posts}}
    {{> "post-card"}}
  {{/foreach}}

  {{pagination}}
</div> <!-- /mCont -->

{{#contentFor "headerTitle"}}
  <h1>
    <a href="{{@site.url}}" title="{{@site.title}}">{{@site.title}}</a>
  </h1>
{{/contentFor}}
